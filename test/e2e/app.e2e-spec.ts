import { ReleaseTourPage } from './app.po';

describe('App', () => {
  let page: ReleaseTourPage;

  beforeEach(() => {
    page = new ReleaseTourPage();
  });

  //it('should display message saying app works', () => {
  //  page.navigateTo();
  //  expect(page.getParagraphText()).toEqual('app works!');
  //});
});
